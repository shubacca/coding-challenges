## Simple Calculator App

This is a simple calculator app that can perform addition, subtraction, multiplication, and division operations.

To run this app, run the following command from terminal:
`python3 calculator/calcu.py` from the main directory, and input the expression you want to evaluate.